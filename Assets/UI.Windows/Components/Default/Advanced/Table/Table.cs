using UnityEngine;
using UnityEngine.Extensions;
using System.Collections.Generic;
using UnityEngine.Events;
using System.Linq;

namespace UnityEngine.UI.Windows.Components.Table {
	
	public class Table : Components.List {

		/*private List<WindowComponent> list = new List<WindowComponent>();

		public WindowComponent headerSource;

		//[SerializeField]
		//private int columns;

		public void SetColumnsCount(int columns) {

			//this.columns = columns;

		}

		public T AddItem<T>(int column) where T : WindowComponent {

			var instance = this.source.Spawn();
			instance.Setup(this.GetLayoutRoot());

			this.list.Add(instance);
			
			instance.gameObject.SetActive(true);

			return instance as T;

		}*/

	}
	
}